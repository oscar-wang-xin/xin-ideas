---
name: xin-ideas
description: "灵感收集与多维度体检 + 可落地拆解工具。当用户抛出新想法/灵感、希望先记录并评估（而非立即实现）时使用：对想法做八维评价（解决问题/实用性/市场价值/竞品分析/变现可能/技术方案/可扩展性/实现计划）并 1-10 打分，同时给出好处/坏处、可能遇到的问题与应对、分阶段落地路线图、要避免的坑、成功失败判据与成本投入（精力/时间/金钱），生成精美 HTML 详情页（.xin-ideas/idea_<时间戳>.html）与索引页（idea.html，支持搜索、排序、点击跳转）。触发词与场景：「收集灵感」「记个想法」「灵感库」「xin-ideas」「记一笔」「做个灵感评估」「把想法存一下」「给这个想法打个分」。本 skill 只做评估与归档，不实现想法。"
---

# xin-ideas · 灵感体检与可落地拆解

把想法抛进来，先体检、后动手。每个灵感都产出一份「八维评分 + 可落地拆解」的精美 HTML 详情页，并在索引页形成可检索、可排序、可跳转的卡片。

**核心原则：拆解必须具体、可落地、能量化。禁止只写方向、概念、大词。** 每一条都要能回答「具体做什么、拿什么衡量、花多少」。

## 产物约定（相对当前工作区根目录）

- `.xin-ideas/` 目录：每个灵感一个详情页 `idea_<时间戳>.html`
- `idea.html`：索引页（灵感卡片墙），位于工作区根目录、`.xin-ideas/` 的上层
- 时间戳格式：`YYYYMMDD_HHMMSS`（用 `date +%Y%m%d_%H%M%S` 取本地时间）

## 工作流

### 1. 接收想法
从用户消息中提取灵感核心（一句话能讲清它在解决什么）。信息不足时先复述要点并合理假设，**竞品与市场务必联网核实**（遵循「批量并行联网查、撞车即弃」原则），不凭记忆硬写。

### 2. 八维评分（1-10 整数，每维附一段评价）
综合评分 = 八维均值（保留一位小数）。

| 维度 | 评估要点 |
|------|----------|
| 解决问题 | 痛点是否真实、紧迫、被广泛感知 |
| 实用性 | 在日常/工作场景中可被使用的程度与频率 |
| 市场价值 | 目标人群规模、付费意愿、市场阶段 |
| 竞品分析 | 已有玩家、其短板、本想法的差异化与壁垒（空白/强壁垒给高分） |
| 变现可能 | 变现路径清晰度与可行性（订阅/一次性/渠道替卖等） |
| 技术方案 | 实现思路与关键技术选型的可行性 |
| 可扩展性 | 未来可延展的方向、复用与组合空间 |
| 实现计划 | 能否拆成 MVP→加载背书→放大 的分阶段步骤 |

评级：≥8.5 S / ≥7.5 A / ≥6.5 B / ≥5.5 C / 其余 D。

### 3. 可落地拆解（必填，全部产出）
逐项填写，标准是「照着这份拆解，另一人也能开工」：

- **pros 好处**：具体收益，尽量算账（省下多少时间/多少钱、减少多少返工、带来什么可度量的价值）。避免「提升效率」这类空话。
- **cons 坏处与代价**：真实成本与副作用（时间、金钱、精力、机会成本、维护负担、可能的负面后果），要敢说难处。
- **risks 可能遇到的问题**：每条是 `{问题, 应对}`。问题要具体到场景（如「微信对公众号正文有反爬验证」），应对要可执行（如「用 X 方案绕过 / 退而求其次抓备用源」）。
- **roadmap 落地路线图**：分阶段（如 第0步·验证 → 第1步·MVP → 第2步·自用跑通 → 第3步·加载背书/放大）。每阶段给出 `目标 goal`、`可执行动作 steps[]`、`完成标志 done`。动作要是能直接动手做的事。
- **avoid 要避免的坑**：最容易犯且代价高的错误，一条条列清（如「别一上来就追求通用，先垂直自用」）。
- **metrics 成功/失败判据**：`{指标, 成功线, 止损线}`。必须是可在过程中随时评判的量化标准（数字、时间点、完成度），让「该继续还是该停」一眼可判。
- **cost 成本投入**：`{effort:{v,n}, time:{v,n}, money:{v,n}, other}` —— 精力（人日）、时间（周期）、金钱（量级/预算）、其它代价（机会成本等）。

### 4. 生成详情页
1. 复制 `assets/templates/detail.html` 到工作区 `.xin-ideas/idea_<时间戳>.html`
2. 只改文件末尾 `<script>` 里的 `IDEA` 对象字段（结构见模板顶部注释）：
   - `title` / `ts` / `tags`（数组，如 `["工具","AI","内容线"]`）/ `summary`（一句话结论）
   - `scores`：八个维度分数；`details`：八维评价正文（多段用空行 `\n\n` 分隔）
   - `pros` / `cons` / `avoid`：字符串数组
   - `risks` / `roadmap` / `metrics`：对象数组（字段见模板注释）
   - `cost`：`{effort,time,money,other}`
3. 不要改动其余 CSS/JS 与渲染逻辑。缺失字段对应板块会自动隐藏。

### 5. 更新索引页
- 若工作区根目录无 `idea.html`：复制 `assets/templates/index.html` 到根目录。
- 在 `idea.html` 末尾 `<script>` 的 `IDEAS` 数组里追加一项：
  ```js
  { id:"<时间戳>", title:"...", ts:"YYYY-MM-DD HH:MM", tags:["..."],
    score:<综合均分>, summary:"...", file:".xin-ideas/idea_<时间戳>.html" }
  ```
- `score` 即步骤 2 的综合评分；`file` 为相对索引页的路径。统计数字由脚本自动重算。

### 6. 反馈用户
告知：已归档、综合评分与评级、一句话结论、以及成本量级与最主要的一条判据；提示在 `idea.html` 可搜索/排序/点开详情。不要替用户决定要不要做，只给体检 + 拆解报告。

## 内容质量红线（务必遵守）
- 禁止用「提升效率」「赋能」「打造闭环」等空词充当内容；每条结论都要有具体动作、数字或判据支撑。
- 成本与判据必须量化到可感知量级（人日 / 周 / 元 / 百分比 / 时间点），不接受「很快」「不多」。
- 坏处与风险要写真实的难处，不做报喜不报忧的包装。
- 拿不准的市场/竞品信息先联网查，查不到就标注「待核实」，不编造数据。

## 设计基线
详情页与索引页均为**纯静态自包含**（不依赖外部 CDN/字体），浅色现代科技风、蓝紫主色（`#6C63FF`/`#3ECFCF`）。详情页含雷达图（内联 SVG+JS 绘制）、分项打分条、好处/坏处对照、问题与应对、时间线路线图、要避的坑、判据表、成本卡。索引页含卡片墙、实时搜索、排序下拉。模板已美化，改动时只动数据对象。

## 注意事项
- 本 skill 只评估与记录，不落地实现想法（除非用户另行要求）。
- 分类标签自由，建议与灵感空间既有方向（如 工具/内容/圈层/医疗/AI）保持一致，便于检索。